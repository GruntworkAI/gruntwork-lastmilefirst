---
name: run-scan-secrets
description: Scan repos for secrets, credentials, and sensitive data
argument-hint: "[--all | --audit [--github] | --install-hooks | --add-format | --list-formats]"
---

Load and execute the `scan-secrets` skill for full implementation.

Modes: scan (default), --all (workspace), --audit (hygiene), --install-hooks (pre-commit), --add-format (custom rules), --list-formats (show rules)
